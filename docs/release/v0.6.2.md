# v0.6.2

This patch release keeps the AI-friendly, passwordless macOS install path and
hardens the GitHub Release workflow.

## Changes

- Keeps the default macOS instructions on the release tarball and
  `$HOME/.local/bin`, avoiding `sudo` and password prompts.
- Updates the release workflow to use a dedicated GitHub Release action for
  release creation and asset upload.
