# v0.6.10

This patch release improves Feishu delivery reliability when the OpenAPI access token becomes invalid.

## Changes

- Rebuilds the Feishu OpenAPI client and refreshes the tenant access token after invalid-token send failures.
- Retries the original Feishu send once after successful auth recovery.
- Keeps auth recovery failures visible instead of falling back to legacy card rendering.
