# v0.6.11

This patch release improves recovery after the computer goes offline and reconnects.

## Changes

- Supervises the Feishu websocket callback connection and rebuilds it after disconnects.
- Makes local `ctr-go repair` restart the macOS LaunchAgent when the service is loaded.
- Keeps the old in-daemon repair request as a fallback when the service is not loaded.
