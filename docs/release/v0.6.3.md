# v0.6.3

This patch release improves the Feishu setup authorization flow.

## Changes

- Writes the Feishu setup QR code as a PNG image in the temp directory.
- Prints a Markdown image reference for UIs that can render local images.
- Keeps the existing terminal QR code and `--no-qr` behavior.
